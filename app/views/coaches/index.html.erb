<p id="notice"><%= notice %></p>

<div class="container">
  <%= link_to 'Add Coach', new_coach_path %>
  <h1>Coaches</h1>

  <div class="card-deck mb-3 row">
      <% @coaches.each do |coach| %>

        <%= render partial: "coach", locals: {coach: coach, with_edit_links: true}  %>
      <% end %>
  </div>
</div>


