<div class="container">
    <% if user_signed_in? %>
        <div class="row m-2">
            <div class="col-12">
                <%= link_to "New Post", posts_path, method: :post, class:"btn btn-primary float-right " %>
            </div>
        </div>
    <% end %>
   <% @posts.each do |post| %>
     <div class="row mt-5 mb-5">
        <%= render partial: 'post_short', locals: { post: post } %>
        <% if user_signed_in? %>
          <%= render partial: 'post_admin_info', locals: { post: post } %>
        <% end %>
     </div>
     <hr>
   <% end %>
</div>
